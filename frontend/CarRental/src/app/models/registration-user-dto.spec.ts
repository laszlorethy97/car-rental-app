import { RegistrationUserDto } from './registration-user-dto';

describe('RegistrationUserDto', () => {
  it('should create an instance', () => {
    expect(new RegistrationUserDto()).toBeTruthy();
  });
});
